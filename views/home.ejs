<%- include("./partials/header") -%>

<h1>Home</h1>
<p><%= homeStartingContent %></p>

<!-- <% for (let post of posts) { %>
<h2><%= post.title %></h2>
<p><%= post.story %></p>
<% } %>  -->
<% posts.forEach(function(post) { %>
<h1><%= post.title %></h2>
<p><%= post.story.substring(0, 100) %></p>
<% }); %> 
<%- include("./partials/footer") -%>
